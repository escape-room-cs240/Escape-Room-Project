import java.util.*;
public class itemTest{
   public static void main(String[] args){
      Item item = new Item("lock", "This hasn't been used in a while, it's rusty. Looks like it could shatter if a key uses it.");
      Item item2 = new Item("apple");
      Item item3 = new Item("old lock");
      Item item4 = new Item("old key");
      Set<Item> = new TreeSet<Item>();
      System.out.println(item.getDescription());
      System.out.println(item2.getDescription());
      System.out.println(testKey.doesUnlock(testLock));
   }
}